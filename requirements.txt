# Versions below are the ones the released results were actually produced with
# (Python 3.11.9, Windows 11). Minor version drift is normally harmless, but
# resampling and tree ensembles are seeded through scikit-learn's RNG, so a
# different scikit-learn or imbalanced-learn release can shift individual fold
# scores slightly. Pin these to reproduce results/ exactly.

numpy==2.4.6
pandas==3.0.3
scipy==1.17.1
scikit-learn==1.9.0
imbalanced-learn==0.14.2
xgboost==3.2.0
shap==0.51.0
optuna==4.9.0

# analysis and figures
matplotlib==3.11.1
statsmodels==0.14.6
scikit-posthocs==0.14.0
pillow==12.3.0
